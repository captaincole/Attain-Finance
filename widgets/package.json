{
  "name": "personal-finance-widgets",
  "version": "1.0.0",
  "description": "ChatGPT widgets for Personal Finance MCP",
  "type": "module",
  "scripts": {
    "build": "esbuild src/financial-summary.tsx src/account-status.tsx --bundle --format=esm --outdir=../public/widgets",
    "build:css": "esbuild src/styles/base.css --bundle --outfile=../public/widgets/widget-base.css && esbuild src/styles/financial-summary.css --bundle --outfile=../public/widgets/financial-summary.css && esbuild src/styles/account-status.css --bundle --outfile=../public/widgets/account-status.css",
    "build:budget-list": "esbuild src/budget-list.tsx --bundle --format=esm --outfile=../public/widgets/budget-list.js",
    "build:all": "npm run build && npm run build:css && npm run build:budget-list",
    "watch": "npm run build -- --watch",
    "preview": "node preview-server.js",
    "dev": "npm run build:all && npm run preview"
  },
  "devDependencies": {
    "@types/node": "^24.9.2",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "esbuild": "^0.19.0",
    "express": "^5.1.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
